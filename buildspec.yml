version: 0.2

phases:
  install:
    runtime-versions:
      php: 7.4
  build:
    commands:
      - echo "Build phase: Installing dependencies"
      - composer install
      - echo "Build phase: Running tests (if any)"
  post_build:
    commands:
      - echo "Post-build phase: Deploying to your target (e.g., AWS Elastic Beanstalk)"
